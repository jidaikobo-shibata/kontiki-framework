checks:
  php: true
    undefined-method: false

build:
  environment:
    php:
      version: "8.2"
  nodes:
    analysis:
      tests:
        override: [php-scrutinizer-run]

filter:
  paths: ["src/*"]
